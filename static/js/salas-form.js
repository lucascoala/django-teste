$(document).ready(function() {
    // Outras inicializações específicas para o formulário de salas podem ser adicionadas aqui
    // A inicialização do Select2 foi movida para o template para evitar duplicação
});